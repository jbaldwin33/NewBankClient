<framework:ViewBaseControl x:Class="NewBankWpfClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NewBankWpfClient.Views"
             xmlns:pwd="clr-namespace:MVVMFramework.Utilities;assembly=MVVMFramework"
             xmlns:framework="clr-namespace:MVVMFramework.Views;assembly=MVVMFramework"
             xmlns:controls="clr-namespace:MVVMFramework.Controls;assembly=MVVMFramework"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
    </Grid.RowDefinitions>
    <controls:DefaultStackPanel Visibility="{Binding LoggedIn, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource BoolToVisConverter}}">
      <controls:SmallButton Content="{Binding LogoutLabel}" Command="{Binding LogoutCommand}"/>
    </controls:DefaultStackPanel>
    <controls:DefaultStackPanel Visibility="{Binding LoggedIn, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource InvBoolToVisConverter}}">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="auto"/>
          <RowDefinition Height="auto"/>
          <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <controls:DefaultStackPanel Grid.Row="0">
          <controls:SmallTextBlock Text="{Binding UsernameLabel}"/>
          <controls:MediumTextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" LostFocus="MediumTextBox_LostFocus"/>
        </controls:DefaultStackPanel>
        <controls:DefaultStackPanel Grid.Row="1">
          <controls:SmallTextBlock Text="{Binding PasswordLabel}"/>
          <PasswordBox x:Name="pBox" pwd:PasswordBoxMVVMAttachedProperties.EncryptedPassword="{Binding SecurePassword, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="100" Height="25" FontSize="20" PasswordChanged="PasswordBox_PasswordChanged"/>
        </controls:DefaultStackPanel>
        <controls:DefaultStackPanel Grid.Row="2">
          <controls:SmallButton Content="{Binding LoginLabel}" Command="{Binding LoginCommand}"/>
        </controls:DefaultStackPanel>
      </Grid>
    </controls:DefaultStackPanel>
  </Grid>
</framework:ViewBaseControl>
